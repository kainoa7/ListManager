{
  "name": "listmanager",
  "version": "1.0.0",
  "description": "- Create a project folder and make it the active folder.\r - Download the SimpleList - Starter repo from GitHub into your project folder.\r - Initialize the project folder as a Node project.\r - Initialize the project folder as a GitRepo\r - Create a file named `.gitignore` and add `node_modules/` to it.\r - Install the `Express` module.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  }
}
